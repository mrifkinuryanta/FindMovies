<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav_graph"
    app:startDestination="@id/movieFragment">

    <fragment
        android:id="@+id/movieFragment"
        android:name="com.dev.divig.findmovies.ui.movie.MovieFragment"
        android:label="MovieFragment"
        tools:layout="@layout/fragment_movie" />

    <fragment
        android:id="@+id/tvShowFragment"
        android:name="com.dev.divig.findmovies.ui.tvshows.TvShowsFragment"
        android:label="fragment_tv_show"
        tools:layout="@layout/fragment_tv_shows" />

    <fragment
        android:id="@+id/favoriteFragment"
        android:name="com.dev.divig.findmovies.ui.favorite.FavoriteFragment"
        android:label="fragment_favorite"
        tools:layout="@layout/fragment_favorite" />
</navigation>